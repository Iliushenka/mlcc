event<join> {
    `anything` = 1 + 1
    `variable` = 2 + 2 * 2
    `another` = (2 + 2) / 2
    `star` = 2 * 2 * 2 * 100 / 100 + 10
    # скоро сделаю функции и перенос строки если она переполнена в другую функцию без задержки
}

event<leave> {
    # просто пример события на данный момент их два так как код в разработке и до добавления
    # других событий еще очень далеко
    # если есть идеи пишите в дискорд iliushenka :3

    # так же надеюсь сейчас ведутся разработка мода иначе эхэхэх
}